Problem: Given a non-empty array of integers nums, every element appears twice except for one. Find that single one.
Compare each element of array with every element 
If both the elements are equal, then increase the count by 1
if count value is equal to 1 return the element in a[i]
